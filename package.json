{
  "name": "iot-project-visualisation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "server": "nodemon index.js --verbose",
    "dev": "start http://localhost:5000 && npm run server",
    "update": "npm install --verbose && npm update --verbose",
    "deploy": "npm run update && git add . --verbose && git commit -m \"aws deploy\" --verbose && git pull --verbose && git push origin master --verbose && eb deploy --verbose && eb open --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kronos292/IoT-Project-Visualisation.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/kronos292/IoT-Project-Visualisation/issues"
  },
  "homepage": "https://github.com/kronos292/IoT-Project-Visualisation#readme",
  "dependencies": {
    "express": "^4.16.3",
    "nodemon": "^1.17.3"
  }
}
